describe('ConstructorIO', () => {
  // Should return an instance when valid API key is provided

  // Should error when invalid API key is provided

  // Should error when no options are provided

  // Should error when no API key is provided
});
